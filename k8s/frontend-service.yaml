apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  labels:
    app: placement-frontend
spec:
  type: LoadBalancer
  selector:
    app: placement-frontend
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  loadBalancerIP: ""  # Will be assigned by GCP
